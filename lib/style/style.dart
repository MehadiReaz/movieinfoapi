import 'package:flutter/material.dart';

const backgroundcolor = Color(0xFF15141F);
const colorWhite = Color.fromRGBO(255, 255, 255, 1);
TextStyle textStyle(double fontsize, fontweight) {
  return TextStyle(
    color: colorWhite,
    fontSize: fontsize,
    fontWeight: fontweight,
  );
}
